Known BUGS:
- SVG render errors:
  - other/mal-rendered objects
    - clock ("shine")
  - shading/semi-transparency:
    - apple
    - jar
    - necklace
- Wait/Loading animation not centered vertically
- Appears to be a delay when frame is closed from native "X" while sound is playing

Bugs to report:
- Inkscape:
  - "Title" meta data loss when converting old SVG
