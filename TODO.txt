Priority:
- Fix semi-transparent shading in SVG:
  - electric guitar
  - magnifying glass
  - necklace
  - tomato
- Report wxSVG render bug when objects grouped multiple times
- Report wxSVG object order rendering bug (cat.svg)
- Report Inkscape "Title" meta data loss when converting old SVG
- Add debugging symbols
- Check for errors when trying to play non-existent sound file

Non-priority:
- Convert audio to OGG
- Use wxThread class instead of pthreads
- Fix app icons on Windows
- Fix/Add UI manifest on Windows
- Improve loading time of SVG images (ex. quarter.svg loads slow)
  - Possible options:
    - Try to optimize SVG documents
    - Cache images at time of setting game mode
    - Export slow-loading SVGs to PNG
- PNG & SVG hats are different colors
- PNG & SVG jars are different colors
- cache images at startup
- Update README
- Find/Create missing SVGs:
  - ribbon
- Clean up include statements
- Convert failsafe image to SVG
- Use config to determine environment (locatation of data files, etc.)
- Edit wheels white BG in bicycle SVG
- Change strawberry SVG
- Remove shading from watch SVG
- Replace xylophone instrument with xun
