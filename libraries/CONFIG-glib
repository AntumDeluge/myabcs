# Depends on:
#  libintl (gettext), libiconv, zlib, libffi

# Update available (not compatible with current patches): 2.61.2

VER_SHORT="2.60"
VER="${VER_SHORT}.4"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="https://download.gnome.org/sources/glib/${VER_SHORT}/${FNAME}"
CONFIG_OPTS="-Dinstalled_tests=false -Dforce_posix_threads=true"
LIBS="-liconv"
LDFLAGS+=" ${LIBS}"
CMD_CONFIG=(meson ${CONFIG_OPTS})
CMD_BUILD=(ninja)
CMD_INSTALL=(ninja install)
