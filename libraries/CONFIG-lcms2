# Depends:
#  libjpeg (libjpeg-turbo), libtiff
# Optional depends:
#  zlib, pthreads

VER="2.9"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.gz"
SOURCE="https://downloads.sourceforge.net/lcms/lcms/${VER}/${FNAME}"
LIBS="-llzma"
