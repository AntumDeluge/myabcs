# Depends on:
#  libogg

VER="1.3.6"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="http://downloads.xiph.org/releases/vorbis/${FNAME}"
# FIXME: does this get passed to make?
OGG_LIBS="${INSTALL_PREFIX}/lib/libogg.a"
