# Depends on:
#  zlib

VER="3.1.2"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.bz2"
SOURCE="https://github.com/wxWidgets/wxWidgets/releases/download/v${VER}/${FNAME}"
CONFIG_OPTS=" --disable-gtktest --disable-sdltest --with-libpng=sys --with-libjpeg=builtin \
--with-libtiff=builtin --with-libxpm=builtin --with-libiconv=sys --with-regex=builtin \
--with-zlib=sys --with-expat=sys"
if ${OS_WIN}; then
	CONFIG_OPTS+=" --with-msw"
fi
