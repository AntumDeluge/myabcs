
ver="2.2.7"
dname="${name}-${ver}"
fname="${dname}.tar.bz2"
src="https://github.com/libexpat/libexpat/releases/download/R_$(echo ${ver} | sed -e 's|\.|_|g')/${fname}"

config_opts=(
	--without-examples
	--without-tests
	--without-docbook
)
