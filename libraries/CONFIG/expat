VER="2.2.7"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.bz2"
SOURCE="https://github.com/libexpat/libexpat/releases/download/R_$(echo ${VER} | sed -e 's|\.|_|g')/${FNAME}"

CONFIG_OPTS=(
	--without-examples
	--without-tests
	--without-docbook
)
