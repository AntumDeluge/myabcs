# Depends on:
#  pkg-config, libintl (gettext), expat | libxml2, freetype
# Optional depends
#  libiconv, jsonc/json-c?

DEPENDS=(freetype gperf libxml2)

VER="2.13.91"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="https://www.freedesktop.org/software/fontconfig/release/${FNAME}"

CONFIG_OPTS=(
	--enable-iconv
	--disable-docs
)

if ${OS_WIN}; then
	# FIXME: not sure why I have to explicitly link to static harfbuzz
	LIBS="-l:libharfbuzz.a"
fi
