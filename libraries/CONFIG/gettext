# Depends on:
#  libiconv
# Optional depends:
#  pthreads | pth, ncurses, termcap, bzip2, xz, libxml2

VER="0.20.1"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="https://ftp.gnu.org/pub/gnu/gettext/${FNAME}"
CONFIG_OPTS="--disable-curses --with-included-libxml --with-included-regex --with-included-libunistring --without-xz"
