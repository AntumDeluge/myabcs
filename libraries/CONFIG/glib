# Depends on:
#  libintl (gettext), libiconv
# Optional depends
#  libmount (util-linux), pcre

# Update available (not compatible with current patches): 2.61.2

VER="2.60.4"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="https://download.gnome.org/sources/glib/${VER%.*}/${FNAME}"
CONFIG_OPTS=(-Dinstalled_tests=false -Dforce_posix_threads=true -Dinternal_pcre=true)
CMD_CONFIG=(meson ${CONFIG_OPTS})
CMD_BUILD=(ninja)
CMD_INSTALL=(ninja install)
