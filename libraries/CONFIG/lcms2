# Depends:
#  libjpeg (libjpeg-turbo), libtiff
# Optional depends:
#  zlib, pthreads

ver="2.9"
dname="${name}-${ver}"
fname="${dname}.tar.gz"
src="https://downloads.sourceforge.net/lcms/lcms/${ver}/${fname}"
libs="`pkg-config libtiff-4 --libs --static`"
