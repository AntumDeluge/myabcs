# Depends:
#  pkg-config, glib, libintl (gettext)
# Optional depends:
#  gtk-doc

DEPENDS=(pkg-config libxml2)

VER="0.6.13"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="https://download.gnome.org/sources/${NAME}/${VER%.*}/${FNAME}"
LIBS="-lintl"
if ${OS_WIN}; then
	LIBS+=" -lole32"
fi
