# Depends:
#  pkg-config, glib, libintl (gettext), libxml2
# Optional depends:
#  gtk-doc

depends=(libxml2)

ver="0.6.13"
dname="${name}-${ver}"
fname="${dname}.tar.xz"
src="https://download.gnome.org/sources/${name}/${ver%.*}/${fname}"

libs="-lintl"

if ${os_win}; then
	libs+=" -lole32"
fi
