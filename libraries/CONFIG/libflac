# Depends on:
#  libiconv
# Optional depends:
#  libogg, nasm

DEPENDS=(nasm libogg)

VER="1.3.2"
DNAME="${NAME}-${VER}"
EXTRACT_NAME="flac-${VER}"
FNAME="${EXTRACT_NAME}.tar.xz"
SOURCE="http://downloads.xiph.org/releases/flac/${FNAME}"

CONFIG_OPTS=(
	--disable-doxygen-docs
	--disable-xmms-plugin
	--disable-cpplibs
	--enable-ogg
)
