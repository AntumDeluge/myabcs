# Depends:
#  nasm
# Optional depends:
#  java

VER="2.0.2"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.gz"
SOURCE="https://downloads.sourceforge.net/libjpeg-turbo/${VER}/${FNAME}"
CMD_CONFIG=(cmake)
CONFIG_OPTS=(-DWITH_JPEG8=ON -DWITH_TURBOJPEG=OFF)
LIBTYPE_OPTS=(-DENABLE_STATIC=ON -DENABLE_SHARED=OFF)
