# Optional depends:
#  zlib, libjpeg (libjpeg-turbo), jbigkit, liblzma (xz), libzstd, libwebp

ver="4.0.10"
extract_name="tiff-${ver}"
dname="lib${extract_name}"
fname="${extract_name}.tar.gz"
src="https://download.osgeo.org/libtiff/${fname}"
config_opts=(--disable-zstd --disable-webp --disable-cxx)
