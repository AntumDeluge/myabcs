# Depends:
#  pkg-config, zlib, liblzma (xz)
# Optional depends:
#  libiconv, icu, python, readline
# Unsure depends:
#  libintl (gettext)

DEPENDS=(pkg-config zlib libiconv)

VER="2.9.9"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.gz"
SOURCE="http://xmlsoft.org/sources/${FNAME}"

CONFIG_OPTS=(
	--without-python
	--without-debug
	--without-lzma
)
