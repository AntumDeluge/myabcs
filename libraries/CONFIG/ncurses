# Optional depends:
#  pkg-config, libtool, termcap, pthreads
#
# Notes:
#  - newer versions may be available from https://invisible-island.net/ncurses/

ver="6.1"
dname="${name}-${ver}"
fname="${dname}.tar.gz"
src="ftp://ftp.gnu.org/gnu/${name}/${fname}"
config_opts=(--without-manpages --enable-pc-files --without-debug --enable-termcap --enable-widec --with-pthread)
libtype_opts=(--without-shared --with-normal)
