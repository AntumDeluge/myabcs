
# Depends:
#  glib
# Unsure depends:
#  pkg-config (self?)
#
# Notes:
#  - configure script utilizes `cmp` command found in diffutils package

ver="0.29.2"
dname="${name}-${ver}"
fname="${dname}.tar.gz"
src="https://pkgconfig.freedesktop.org/releases/${fname}"

config_opts=(
	--with-internal-glib
	--with-libiconv
)
