# Depends:
#  libiconv, libintl
# Optional depends:
#  gzip, bzip2, lzip, lzop, xz, zstd

DEPENDS=(gzip bzip2 lzip lzop, xz, zstd)

VER="1.32"
DNAME="${NAME}-${VER}"
FNAME="${DNAME}.tar.xz"
SOURCE="https://ftp.gnu.org/gnu/${NAME}/${FNAME}"
