# Depends:
#  pkg-config, python, libiconv, libintl (gettext)
# Optional depends:
#  pcre|pcre2, pth|pthreads|winthreads, libpsl, gnutls|openssl, zlib, libunistring, libidn, libunicode,
#  libuuid, gpgme, metalink

depends=(pkg-config)

ver="1.20.3"
dname="${name}-${ver}"
fname="${dname}.tar.lz"
src="https://ftp.gnu.org/gnu/${name}/${fname}"
