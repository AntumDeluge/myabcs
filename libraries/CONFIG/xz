# Depends:
#  libiconv, libintl (gettext)
# Unsure optional depends:
#  pthreads

ver="5.2.4"
dname="${name}-${ver}"
fname="${dname}.tar.xz"
src="https://tukaani.org/${name}/${fname}"
config_opts=(--disable-xz --disable-xzdec --disable-lzmadec --disable-lzmainfo --disable-lzma-links --disable-scripts --disable-doc)
